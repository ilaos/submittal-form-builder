name: deploy

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Create build marker
        run: |
          echo "$(date -u +'%Y-%m-%dT%H:%M:%SZ') $GITHUB_SHA" > build.txt

      - name: Install lftp (for SFTP mirror)
        run: sudo apt-get update && sudo apt-get install -y lftp

      - name: Explore SFTP directory structure
        env:
          SFTP_HOST: ${{ secrets.SFTP_HOST }}
          SFTP_USERNAME: ${{ secrets.SFTP_USERNAME }}
          SFTP_PASSWORD: ${{ secrets.SFTP_PASSWORD }}
        run: |
          set -e
          echo "Exploring directory structure on GoDaddy SFTP server..."
          # Create a list of commands to explore common GoDaddy WP paths
          cat > sftp_cmds.txt <<'EOF'
          pwd
          ls -la /
          ls -la /home || true
          ls -la /html || true
          ls -la /public_html || true
          ls -la /wp-content || true
          ls -la /wp-content/plugins || true
          ls -la /wp-content/plugins/submittal-spec-sheet-builder || true
          ls -la / || true
          EOF

          # Run batch SFTP command
          lftp -u "${SFTP_USERNAME},${SFTP_PASSWORD}" sftp://${SFTP_HOST} -e "
            set sftp:auto-confirm yes;
            set net:max-retries 2;
            set net:timeout 20;
            source sftp_cmds.txt;
            bye;
          "

      # Deploy to the EXACT Managed WP plugin path
      - name: Deploy plugin via SFTP mirror
        env:
          SFTP_HOST: ${{ secrets.SFTP_HOST }}
          SFTP_USERNAME: ${{ secrets.SFTP_USERNAME }}
          SFTP_PASSWORD: ${{ secrets.SFTP_PASSWORD }}
        run: |
          set -e
          REMOTE_DIR="/wp-content/plugins/submittal-spec-sheet-builder"
          echo "Deploying to sftp://${SFTP_HOST}${REMOTE_DIR}"

          lftp -e "
            set sftp:auto-confirm yes;
            set net:max-retries 2;
            set net:timeout 20;
            open -u ${SFTP_USERNAME},${SFTP_PASSWORD} sftp://${SFTP_HOST};
            mkdir -p ${REMOTE_DIR};
            mirror -R \
              --verbose \
              --only-newer \
              --parallel=4 \
              --exclude-glob .git* \
              --exclude-glob .github/ \
              --exclude-glob node_modules/ \
              ./ ${REMOTE_DIR};
            bye
          "

      # Verify we actually deployed to the plugin folder
      - name: Verify deployed files are in plugin folder
        env:
          SFTP_HOST: ${{ secrets.SFTP_HOST }}
          SFTP_USERNAME: ${{ secrets.SFTP_USERNAME }}
          SFTP_PASSWORD: ${{ secrets.SFTP_PASSWORD }}
        run: |
          set -e
          cat > sftp_verify.txt <<'EOF'
          ls -la /wp-content/plugins/submittal-spec-sheet-builder
          EOF
          sftp -oBatchMode=no -b sftp_verify.txt ${SFTP_USERNAME}@${SFTP_HOST} <<EOF2
          ${SFTP_PASSWORD}
          EOF2 || true
